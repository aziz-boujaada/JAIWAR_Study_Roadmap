PORT=4000

# Required for Vercel deployment and any production deployment that should use a persistent database.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Optional if the frontend is hosted separately from the API.
# VITE_API_URL=